# Given one csv and another csv, and a top X value
# Take the top X words
# Find those words in all of the rows
# If those words exist, get the values
# Subtract the first csvs values from the second csvs values
# Return a csv of X length with the associated values